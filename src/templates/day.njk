<div class="day {{ day.name }}">
  <div class="day__title">
    {{ day.title }}
  </div class="day__agenda">
    {% for event in day.events %}
      {{ event | safe }}
    {% endfor %}
  </div>
</div>